%h1 Landings#index
- if user_signed_in?
	-if current_user.is_employer
		%h1 Welcome to the Employer Home Page!
		=current_user.username
		%p .........
	-else
		%h1 Welcome to the Worker Home Page!
		=current_user.username
		%p .........
-else
	%p welcome
	= link_to "Find a job", new_user_registration_path(:employee => 1), :class => "btn btn-primary btn-lg"
	= link_to "Post a job", new_user_registration_path(:employer => 1), :class => "btn btn-success btn-lg"
