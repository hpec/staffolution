= form_for @employer, builder: RequiredFormBuilder do |f|
  - if @employer.errors.any?
    #error_explanation
      .alert.alert-danger
        %ul
        - @employer.errors.full_messages.each do |msg|
          %li
            = msg
  %fieldset
    %legend.fieldset-title Basic Info
    .form-group
      = f.label :name, 'Company Name'
      = f.text_field :name
    .form-group
      = f.label :phone
      = f.text_field :phone
    .form-group
      = f.label :email
      = f.email_field :email
    .form-group
      = f.label :website
      .row.no-gutters
        .col-xs-1
          %span.prefix http://
        .col-xs-11
          = f.text_field :website

    .form-group
      = f.label :office_type
      = f.text_area :office_type


  = render partial: "addresses/address_fields", locals: {f: f}

  .form-group
    = f.submit nil, :class => 'btn btn-primary'
